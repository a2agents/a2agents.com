{
  "name": "web",
  "version": "1.0.0",
  "private": true,
  "description": "Official website for A2Agents consulting practice",
  "scripts": {
    "dev": "npx tailwindcss -i ./src/css/input.css -o ./dist/styles.css --watch",
    "build": "npx tailwindcss -i ./src/css/input.css -o ./dist/styles.css --minify && pnpm run copy-assets",
    "copy-assets": "cp -r src/*.html src/robots.txt src/CNAME dist/ 2>/dev/null && cp -r src/js dist/ && mkdir -p dist/assets && cp -r src/assets/* dist/assets/ 2>/dev/null && cp -r src/resume dist/ 2>/dev/null || true",
    "serve": "npx serve dist",
    "lint": "echo 'no lint configured for web'",
    "test": "echo 'no tests configured for web'",
    "typecheck": "echo 'no typecheck configured for web'"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  },
  "keywords": ["consulting", "ai", "human-excellence"],
  "author": "A2Agents",
  "license": "MIT"
}
