{
  "name": "@a2agents/email-sender",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json --noEmit",
    "lint": "echo 'no lint configured for email-sender'",
    "test": "echo 'no tests configured for email-sender'",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
