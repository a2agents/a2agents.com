{
  "name": "@a2agents/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo 'shared package has no build step yet'",
    "lint": "echo 'no lint configured for shared'",
    "test": "echo 'no tests configured for shared'",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
