{
  "name": "a2agents-website",
  "version": "1.0.0",
  "description": "Official website for A2Agents consulting practice",
  "scripts": {
    "dev": "npx tailwindcss -i ./src/css/input.css -o ./dist/styles.css --watch",
    "build": "npx tailwindcss -i ./src/css/input.css -o ./dist/styles.css --minify && npm run copy-assets",
    "copy-assets": "cp -r src/*.html dist/ && cp -r src/js dist/ && mkdir -p dist/assets && cp -r assets/* dist/assets/ 2>/dev/null || true",
    "serve": "npx serve dist"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  },
  "keywords": ["consulting", "ai", "human-excellence"],
  "author": "A2Agents",
  "license": "MIT"
}